import React from 'react';

const Collection = props => {
   //attach redux to get user info and collection id. 
   //map over the list of collection books using the book component through props
   return (
      <div>Collection</div>
   )
}

export default Collection;